workflows:
  build_ios:
    name: Build iOS
    scripts:
      - npm install -g eas-cli
      - npm install
      - echo -e "$EXPO_USERNAME\n$EXPO_PASSWORD" | eas login
      - eas build -p ios
    artifacts:
      - eas-build/**/*.ipa

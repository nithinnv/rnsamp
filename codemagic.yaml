workflows:
  build_ios:
    name: Build iOS
    scripts:
      - npm install -g eas-cli
      - npm install
      - echo -e "machine api.expo.dev\n  login $nithinnv\n  password $thestudent" > ~/.netrc
      - eas build -p ios
    artifacts:
      - eas-build/**/*.ipa
